INSTALLATION

1. Go to the theme´s folder:
2. Install gulp: npm install --global gulp-cli
3. Install dependencies including Bootstrap latest version: npm install

CREATE SCSS FILE

1. Create file inside scss/[directory]/[filename.scss]
2. Include the file in import.scss file

EXECUTION
Just run "gulp"